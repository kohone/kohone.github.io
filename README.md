# kohone.github.io

Сайт-витрина всех приложений: страница, политика, поддержка для каждого. Собирается скриптом:

    python3.14 build.py

Новое приложение — запись в `APPS` и функция страниц в `build.py`, как `nardy_pages`.
